# spec-stream reads the prooph board API key from the environment (never from the config file).
# Create a key as a workspace admin in prooph board (a read-only key is recommended).
PROOPHBOARD_API_KEY=pb_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
